package com.example.CGI_Restaurant.domain.entities;

/** Lifecycle status of a booking (pending, confirmed, cancelled, no-show, completed). */
public enum BookingStatusEnum {
    PENDING, CONFIRMED, CANCELLED, NO_SHOW, COMPLETED
}
