package com.example.CGI_Restaurant.domain.entities;

/** Code identifying a feature (e.g. window, quiet, accessible, near bar). */
public enum FeatureCodeEnum {
    WINDOW, QUIET, PRIVACY, ACCESSIBLE, KIDS_NEARBY, NEAR_BAR, CENTER
}
