package com.example.CGI_Restaurant.domain.entities;

/** Type of zone within a seating plan (e.g. terrace, indoor, bar). */
public enum ZoneTypeEnum {
    TERRACE, INDOOR, PRIVATE, BAR, KIDS, OTHER
}
